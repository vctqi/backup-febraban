// Logo placeholder (exported as a component)
import React from 'react';

const LogoComponent = () => (
  <div style={{
    color: 'white',
    fontWeight: 'bold',
    fontSize: '1.5rem',
    display: 'flex',
    alignItems: 'center'
  }}>
    BANCO DA AMAZÔNIA
  </div>
);

export default LogoComponent;